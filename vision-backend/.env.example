# Reference only: variable names the app reads. Use your EXISTING .env (with API keys, etc.).
# Docker: use the same .env at runtime:  docker run -p 8000:8000 --env-file .env vision-backend:latest
# docker-compose already uses env_file: .env, so no change needed.

# Required
MONGO_URI=
MONGO_DB_NAME=algo_vision_app_cloud

# Optional (Kafka, JWT, Groq, VLM, Gemini, SMTP, etc.) – keep in your real .env
# KAFKA_BOOTSTRAP_SERVERS=
# JWT_SECRET_KEY=
# GROQ_API_KEY=
# ...

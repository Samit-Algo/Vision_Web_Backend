"""
Vision Backend Application — root package.

This package contains the FastAPI app entry point (main.py), API routes,
domain logic, infrastructure (DB, streaming, notifications), and vision
processing pipeline.
"""
